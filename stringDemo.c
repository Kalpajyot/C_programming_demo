#include <stdio.h>
#include <string.h>

int main(){

    char firstName[] = "Kalpajyoti";
    // char name[20];
    char lastName[] = "Hazarika";
    
    // printf(lastName);

    if(strcmp(firstName,lastName) !=0){
            printf("They are not  equal");
    }else{
        printf("They are equal");
    }
    

    // strcpy(name,firstName);
    // strcat(name," ");
    // printf("Name is %s",name);

    // char fullname = strcat("Kalpa","Hazarika");
    // printf("%s",fullname);

}